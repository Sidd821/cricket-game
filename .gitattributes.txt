# =========================================
# Unity .gitattributes
# =========================================

# Treat all text files as UTF-8
* text=auto eol=lf

# Unity YAML text-based assets
*.unity text merge=unityyaml
*.prefab text merge=unityyaml
*.asset text merge=unityyaml
*.meta text merge=unityyaml
*.mat text merge=unityyaml
*.anim text merge=unityyaml
*.controller text merge=unityyaml
*.overrideController text merge=unityyaml
*.physicMaterial text merge=unityyaml
*.physicsMaterial2D text merge=unityyaml
*.guiskin text merge=unityyaml
*.renderTexture text merge=unityyaml
*.shader text merge=unityyaml

# Binary files - do not diff or merge
*.dll binary
*.exe binary
*.png binary
*.jpg binary
*.jpeg binary
*.tga binary
*.tif binary
*.tiff binary
*.gif binary
*.bmp binary
*.psd binary
*.pdf binary
*.wav binary
*.mp3 binary
*.ogg binary
*.mp4 binary
*.mov binary
*.fbx binary
*.obj binary
*.3ds binary
*.blend binary
*.prefab.unity3d binary
*.unitypackage binary

# Ignore line endings in Windows-generated scripts
*.bat text eol=crlf
*.cmd text eol=crlf

# LFS (if used)
# Uncomment these if using Git LFS
# *.psd filter=lfs diff=lfs merge=lfs -text
# *.fbx filter=lfs diff=lfs merge=lfs -text
# *.wav filter=lfs diff=lfs merge=lfs -text
# *.mp4 filter=lfs diff=lfs merge=lfs -text
